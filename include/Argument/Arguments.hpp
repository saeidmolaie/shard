#pragma once

#define ARGUMENT_HELP        "h"
#define ARGUMENT_VERSION     "v"

static const char* ARGUMENTS[] =
{
    ARGUMENT_HELP,
    ARGUMENT_VERSION
};

static const int ARGUMENTS_SIZE = sizeof(ARGUMENTS) / sizeof(ARGUMENTS[0]);